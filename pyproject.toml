[project]
name = "object-tracking-gpu"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Chuanqi Sun", email = "chuanqi.sun.15@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.2",
    "onnx>=1.22.0",
    "opencv-python>=5.0.0.93",
    "supervision>=0.30.0",
    "ultralytics>=8.4.124",
]

[project.scripts]
object-tracking-gpu = "object_tracking_gpu:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
